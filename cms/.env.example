# cms/.env.example

# ==============================================
# 🔐 STRAPI SECRETS (TO BE GENERATED)
# ==============================================
# Generate 4 different secrets with:
# node -e "console.log(require('crypto').randomBytes(64).toString('base64'))"

APP_KEYS=secret1,secret2,secret3,secret4
API_TOKEN_SALT=secret5
ADMIN_JWT_SECRET=secret6
TRANSFER_TOKEN_SALT=secret7
JWT_SECRET=secret8

# ==============================================
# 🌐 SERVER
# ==============================================
HOST=0.0.0.0
PORT=1337

# Public URL of your CMS (to be set after Railway deployment)
PUBLIC_URL=https://cms-production.up.railway.app

# ==============================================
# 🗄️ DATABASE
# ==============================================
# Local: not needed (SQLite automatic)
# Railway: provided automatically by PostgreSQL
# DATABASE_URL=postgresql://user:password@host:port/database
DATABASE_SSL=false

# ==============================================
# 🌍 ENVIRONMENT
# ==============================================
# Automatically set by Strapi/Railway
# NODE_ENV=development  (local)
# NODE_ENV=production   (Railway)

# ==============================================
# 🔒 CORS (already configured in middlewares.js)
# ==============================================
# Allowed origins:
# - https://lacgolbal.up.railway.app (production)
# - http://localhost:5173 (local dev)